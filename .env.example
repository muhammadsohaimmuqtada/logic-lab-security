# Example environment file (do not commit real secrets)

FLASK_ENV=development
FLASK_SECRET_KEY=replace-me
DATABASE_PATH=/var/lib/logic-lab/logiclab.db
ADMIN_USERNAME=dedsec
ADMIN_PASSWORD=replace-me-with-strong-password
# Set TRUST_PROXY=1 only when running behind a properly configured reverse proxy
TRUST_PROXY=0
